# Overseer → 小说写作客户端 — 重构路线图

> 将 AI 动作防火墙改造为人机协作的小说写作系统。
> 保留 TUI 终端交互、HITL 机制、内核/插件架构。
>
> **第一原则：写作心流不可打断。所有设计决策在冲突时，优先保护心流。**

---

## 设计哲学

### 第一原则：心流优先

这份路线图的所有设计决策遵循一条铁律：**写作心流不可打断**。

写作不是编程。编程时被打断确认一个文件权限，回来还能接上思路。写小说时被打断"这段对话要不要这样写？"，作者脑中正在构建的情绪、节奏、画面会瞬间瓦解。恢复心流的成本远高于打断本身。

因此：
- 任何需要打断作者的操作，都必须证明"不打断的后果比打断更严重"
- 系统的默认行为是**静默推进**，而非**暂停请示**
- 结构和一致性检查在后台异步进行，不阻塞写作
- 出错的代价（漏掉一个设定矛盾）远小于打断的代价（摧毁创作状态）

### 第二原则：打开就能写

系统的竞争对手不是另一个写作工具——是一个空白的文本文件。很多作者的工作流就是打开 Markdown 文件开始写。

因此：
- 创建作品后，应该能在 30 秒内开始写第一段话
- 世界观导入、卡片系统、仪表盘是锦上添花，不是前置条件
- 大纲可以后补，不强制在写作前完成
- 最小启动路径：创建作品 → 写

### 核心命题

Overseer 原本解决的问题是"AI 该不该做某件事，谁来判断"。小说写作场景下，这个问题变成：**"这段叙事该不该这样写，谁来判断"**。

两者的底层结构一致——都是一个自主推进的 AI 循环，在关键节点暂停，交由人类决策。区别只在于"推进的是什么"和"决策的依据是什么"。

### 从防火墙到写作系统的语义映射

```
防火墙语义                    写作语义
─────────                    ─────────
AI 发起动作 → 拦截判定        AI 生成叙事 → 一致性审查
风险分级                      创作决策分级
审计日志                      写作历史/版本
自适应规则                    作者偏好学习
```

### 两种笔记方式的融合

系统同时支持两种知识组织方式：

**线性写作**（Plan → Step）：有顺序、有因果的叙事推进，适合"写故事"。

**卡片笔记**（CardPool）：无固定顺序、靠链接产生结构的素材积累，适合"想故事"。

两者的关系是循环的：

```
卡片池 ──素材注入──→ 线性写作
  ↑                      │
  └───── 素材沉淀 ←──────┘
```

写作中产生的好素材回流到卡片池，卡片池中的积累为后续写作提供素材。

**但卡片系统不是写作的前置条件。** 作者先写起来，写到第 5 章发现角色设定记不清了，自然会去用卡片。系统应该在那个时刻恰好提供入口，而不是在第 1 章就强制整理素材。

---

## 数据模型

### 层级结构

当前结构是扁平的 CO 列表。改造后引入三级层次：

```
Novel（作品）
 ├── CardPool（卡片池，挂在 Memory 上）
 └── Storyline（支线，原 CO）
      └── Plan（大纲）
           ├── Step 1（场景单元）
           ├── Step 2
           └── Step 3
```

### Novel（新增模型）

作品是最顶层容器。世界观、角色设定在此层共享，所有支线隶属于同一部作品。

```
Novel
  id: UUID
  title: str                   # 作品名（唯一必填项）
  genre: str (nullable)        # 类型（可稍后补充）
  synopsis: str (nullable)     # 简介/梗概（可稍后补充）
  world_settings: JSON         # 作品级世界观设定（可稍后补充）
  created_at, updated_at

  关系:
  1:N → Storyline
  1:N → Memory（通过 novel_id 隔离）
```

**极简创建**：只需要 title。创建完成后直接进入写作。genre、synopsis、world_settings 都可以在写了几章后再补充——此时作者对自己的故事有了更清晰的认识，补充信息的质量反而更高。

没有 Novel 层的三个问题：
- 世界观是作品级的，不是支线级的。多部小说的设定会互相污染。
- 支线之间的分叉/合并关系无处挂靠。
- 导出单位是作品，不是散落的支线。

**卡片冷启动：世界观导入（可选，不阻塞写作）**

卡片系统的价值依赖密度。新作品的卡片池是空的。但这**不是需要在第一时间解决的问题**——作者先写起来，卡片会自然积累。

世界观导入是可选功能，有两个入口：
1. **创建时可选**：Novel 创建流程中可以跳过，直接开始写
2. **随时补充**：在 Novel 编辑页面中补充世界观描述

```
世界观导入流程（在作者选择启用时触发）：
  1. 作者输入一段自由文本描述世界观和主要角色
  2. 一次 LLM 调用，将自由文本拆解为结构化卡片：
     - world_setting × N（魔法体系/科技水平/社会结构...）
     - character_profile × N（每个提及的角色一张）
     - plot_rule × N（从描述中提取的隐含规则）
  3. 作者审阅卡片列表 → 确认 / 删除 / 编辑
  4. 确认后批量写入 Memory（novel_id = 当前作品）
```

**自然积累路径**：即使不导入世界观，写作过程中的"反向沉淀"（一键提取文本为卡片）和"AI 关联发现"会让卡片池随着写作推进逐渐充实。这条路径更自然——先有故事，后有结构。

复用现有 MemoryExtractor 的 LLM judge 机制。

### Storyline（原 CognitiveObject，扩展）

每个 CO 对应一条故事支线。

```
CognitiveObject 新增字段:
  novel_id: FK → Novel         # 属于哪部作品
  parent_id: FK → self         # 从哪条支线分叉
  branch_point: str            # 分叉点描述
  pov: str                     # 视角（第一/第三人称）
  tone: str                    # 基调（沉重/轻快/讽刺）
  word_count: int              # 累计字数
```

`context` JSON 扩展为叙事结构：

```json
{
  "goal": "支线的核心冲突/走向",
  "world_state": {
    "time": "...",
    "location": "...",
    "active_characters": [],
    "unresolved_tensions": []
  },
  "plot_beats": [
    {"chapter": 1, "beat": "...", "characters": []}
  ],
  "foreshadowing": [
    {"hint": "...", "target_beat": "...", "resolved": false}
  ],
  "character_arcs": {
    "角色名": {"current_state": "...", "arc_direction": "..."}
  }
}
```

CO 状态语义映射：

| 原状态 | 写作语义 |
|--------|---------|
| `created` | 支线已创建，尚未开始写作 |
| `running` | 正在写作中（AI 推进） |
| `paused` | 暂停（人工暂停，或 CONFIRM/APPROVE 级别事件触发） |
| `completed` | 支线写作完成 |
| `aborted` | 支线废弃 |

### Step（原 Execution，扩展）

Step 是场景单元，不是段落（粒度介于段落和章节之间）。

```
Execution 新增字段:
  chapter: int                 # 所属章节
  scene_title: str             # 场景标题
  content_text: Text           # AI 生成的正文
  author_draft: Text           # 作者输入的草稿（人工写模式）
  final_text: Text             # 最终定稿文本
  write_mode: str              # 自动推断，见下表
  accepted: bool               # 是否被作者接受
```

`write_mode` 由系统根据 StepVersion 的 `source` 分布自动推断，作者不需要显式选择：

| 模式 | 推断规则 | 含义 |
|------|---------|------|
| `mostly_human` | StepVersion 中 author_draft/author_edit 占比 > 70% | 这个 Step 主要由作者完成 |
| `mostly_ai` | StepVersion 中 ai_generated/ai_expand 占比 > 70% | 这个 Step 主要由 AI 完成 |
| `mixed` | 两者占比均 < 70% | 人机混合完成 |

**与旧设计的区别**：旧设计中 `write_mode` 是作者在 Step 开始时选择的（author_expand / ai_write / manual_only），这要求每个 Step 都做一次显式选择。新设计中 `write_mode` 是 Step 完成后自动推断的统计结果——因为在自由书写模式下，一个 Step 内可能混合多种操作（先打字写几句，再 Tab 扩写，再 Space 让 AI 续一段），无法在开始时确定模式。

统计功能（如 DashboardScreen 的"AI写/人写"比例）直接查询 StepVersion 的 `source` 字段即可。

### Memory（扩展 category + 新增链接）

Memory 加入 `novel_id` 做作品隔离：

```
Memory 新增字段:
  novel_id: FK → Novel (nullable)
    - novel_id 非空 → 该作品的专属素材
    - novel_id 为空 → 跨作品的通用偏好
```

category 扩展：

| 原有 category | 保留 | 说明 |
|--------------|------|------|
| `preference` | 是 | 作者的写作偏好 |
| `decision_pattern` | 是 | 审阅习惯 |
| `domain_knowledge` | 是 | 通用知识 |
| `lesson` | 是 | 写作经验 |

| 新增 category | 说明 |
|--------------|------|
| `world_setting` | 世界观设定（魔法体系/科技/社会结构） |
| `character_profile` | 角色档案（外貌/性格/动机/关系） |
| `plot_rule` | 剧情规则（"不可杀死主角"/"第3章揭示真相"） |
| `style_guide` | 文风指南（"用短句"/"模仿鲁迅"） |
| `scene_fragment` | 场景碎片/灵感片段 |
| `dialogue_snippet` | 对话片段 |
| `imagery` | 意象/象征物 |

### MemoryLink（新增模型 — 卡片间链接）

```
MemoryLink
  id: UUID
  source_id: FK → Memory
  target_id: FK → Memory
  relation: str    # extends / contradicts / inspires / same_character / same_location
  created_at
```

卡片之间形成网状结构，支持从一张卡片发现关联素材。

### StepCardRef（新增模型 — Step 引用卡片）

```
StepCardRef
  id: UUID
  step_id: FK → Execution
  card_id: FK → Memory
  usage: str       # material / constraint / inspiration
```

作者可以把卡片"钉"到某个 Step 上，AI 生成时参考被钉的卡片内容。

---

## 内核语义迁移

### 迁移原则

内核/插件分离架构不动。三个内核组件做语义重映射，代码结构保留。

划分标准从"移除后是否还是防火墙"变为：**"移除后是否还能保证叙事质量和作者控制权"**。是 → 插件。不是 → 内核。

### FirewallEngine → NarrativeEngine

五层检查从"安全防护"变为"叙事一致性守卫"。**核心原则：所有检查在后台执行，结果以 ⚠ 提示呈现在侧栏，不弹窗打断作者。**

| 层级 | 原语义 | 新语义 | 机制 | 打断策略 |
|------|--------|--------|------|----------|
| L1 | 参数过滤 | **设定一致性** | 内嵌在生成请求中 | 不打断，记录到侧栏 |
| L2 | 循环检测 | **情节重复检测** | 文本相似度，纯 Python | 不打断，侧栏 ⚠ |
| L3 | 权限分级 | **创作决策分级** | 极度偏向 AUTO | 仅角色死亡/违反 plot_rule 时 CONFIRM |
| L4 | 输出沙箱 | **风格沙箱** | 人称/句长检查，纯 Python | 不打断，侧栏 ⚠ |
| L5 | 元认知熔断 | **叙事质量熔断** | 内嵌在生成请求中 | 仅 confidence < 0.3 时 CONFIRM |

创作决策四级分级（极度偏向 AUTO）：

```
AUTO（自动生成，不打断。这是绝大部分内容的默认级别）:
  - 过渡段落（角色移动、场景切换衔接）
  - 环境描写的细节补充
  - 已确定风格的对话续写
  - 新角色首次出场的描写
  - 背景信息补充段落
  - 关键对话（影响角色关系）
  - 情节转折点
  - 伏笔的埋设

NOTIFY（生成后在侧栏 ⚠ 提示，不暂停，作者选择何时查看）:
  - AI confidence 0.3-0.6 的内容
  - 风格沙箱检测到的偏差
  - 情节重复检测告警

CONFIRM（需作者确认，这应该极少触发）:
  - 角色死亡
  - 违反已有 plot_rule 的内容
  - AI confidence < 0.3

APPROVE（需完整预览+审批，几乎不触发）:
  - 支线合并点
  - 结局段落
```

**与原设计的关键区别**：原设计把"关键对话""情节转折""伏笔埋设"放在 CONFIRM，这会导致频繁打断。新设计把这些全部放到 AUTO——因为这些恰恰是作者写作最投入的时刻，打断的代价最高。如果 AI 写得不好，作者可以事后编辑，成本远低于被打断。

### HumanGate → AuthorGate

保留 asyncio.Event 等待机制。**但默认行为从"暂停等待"变为"静默通过"**（`auto_continue=True`）。只有 CONFIRM/APPROVE 级别的事件才触发暂停。

Intent 枚举扩展：

```
现有 Intent:
  APPROVE | REJECT | ABORT | FORCE_ABORT | CONFIRM_COMPLETE | IMPLICIT_STOP | FREETEXT

新增:
  AUTHOR_WRITE      # 作者正在手动输入（通过直接打字触发，不需要菜单选择）
  AI_CONTINUE       # AI 自动续写（通过 Space 触发）
  AI_EXPAND         # AI 基于上文扩写（通过 Tab 触发）
  BRANCH            # 从此处分叉出新支线
  REDIRECT          # 接受内容但改变后续走向
  EDIT              # 编辑 AI 生成的内容
  UNDO              # 撤销上一段 AI 输出
```

**关键改动**：移除 `MANUAL_ONLY` 和 `ORGANIZE_CARDS` 这两个需要暂停的 Intent。人工全写就是直接打字（AUTHOR_WRITE），整理卡片按 `m` 随时进入——都不需要在 Step 开始时做选择。

### PerceptionBus → NarrativePerception

记录/统计的信号从"审批行为"变为"写作行为"：

| 感知维度 | 信号 |
|---------|------|
| 接受率 | 不同内容类型（对话/描写/动作/内心独白）的接受/拒绝率 |
| 叙事节奏 | 连续多段同类型内容时建议变化（全是对话→加入描写） |
| 伏笔追踪 | 埋设时间过长的伏笔提醒回收 |
| 角色平衡 | 某角色消失过久时发出信号 |
| 写作节奏 | 作者选择"人工写"vs"AI写"的比例变化趋势 |

---

## 插件层适配

### LLMPlugin

代码不改，改 PromptPolicy（系统提示词）。从"你是执行AI任务的助手"变为"你是协助人类创作小说的写作伙伴"。

LLMDecision 输出语义变化：

| 原字段 | 新语义 |
|--------|--------|
| `next_action` | 下一个场景的写作方向建议 |
| `tool_calls` | 写作工具调用（write_scene / develop_character 等） |
| `human_required` | 需要作者决策 |
| `confidence` | AI 对当前叙事方向的确信度 |
| `reflection` | 对已写内容的自我评估 |
| `task_complete` | 支线写作完成 |

### PlanPlugin → OutlinePlugin

```
generate_plan()       → 生成章节大纲
subtask               → 场景/Step
checkpoint_reflect()  → 章节结束时的剧情回顾与走向建议
```

**关键**：大纲生成后作者必须能手动编辑——拆分/合并/插入/重排 Step。AI 觉得合适的场景划分和作者脑子里的节奏几乎一定不一样。

### ContextPlugin

最核心的改动。`build_prompt()` 需要组装：

```
当前世界状态
+ 活跃角色档案
+ 未解决伏笔
+ 上几段正文（上下文窗口）
+ 当前 Step 在大纲中的位置
+ 被钉的卡片内容
+ 作者风格偏好
+ NarrativeEngine 输出的约束
```

上下文压缩策略：保留关键剧情节拍和人类决策，压缩过场描写。

**Token 预算分配**：当各类信息争抢 context window 空间时，按固定优先级裁决，而非依赖"智能压缩"。优先级从高到低：

```
NarrativeEngine 输出的约束（硬约束，不可压缩） → 预留 ~500 tokens
当前 Step 大纲 + 被钉的卡片内容                 → 预留 ~1000 tokens
最近 2 个 Step 的 final_text                    → 预留 ~2000 tokens
角色当前状态快照                                 → 预留 ~500 tokens
未解决伏笔列表                                   → 预留 ~300 tokens
远章摘要链                                       → 填充剩余空间
作者风格偏好                                     → 嵌入 system prompt，不占动态空间
```

超出总预算时从底部开始裁剪。具体数值可配置，但优先级顺序固定。这比让 LLM 判断"哪些信息更重要"更可控——写到第 30 章时，第 2 章的伏笔不会因为"太旧"被丢弃。

### ToolPlugin

内置工具替换为写作专用工具集：

| 工具 | 用途 |
|------|------|
| `write_scene` | 生成一个完整场景 |
| `expand_draft` | 基于作者草稿扩写 |
| `develop_character` | 深化角色描写 |
| `create_dialogue` | 生成对话 |
| `describe_setting` | 场景环境描写 |
| `plot_twist` | 生成情节转折建议 |
| `resolve_foreshadowing` | 回收伏笔 |
| `timeline_check` | 检查时间线一致性 |
| `export_chapter` | 导出章节 |

MCP 工具仍可接入（如联网查资料），权限分级不变。

### MemoryPlugin

扩展检索维度：
- 按角色名检索
- 按地点/时间线检索
- 按伏笔状态检索（未解决的伏笔）
- 按卡片链接关系展开关联卡片

---

## Step 交互流程

### 核心改动：从"每步暂停"到"自由书写 + 按需结构化"

原设计的问题：**每个 Step 开始时自动暂停，等待作者选择写作模式。** 这意味着即使作者正在行云流水地写，每过一个 Step 就被迫停下来做一次选择。这个暂停本身就是心流杀手。

新设计的原则：
- **默认行为是"继续"**，而非"暂停等待选择"
- 作者可以随时在"自由书写"和"Step 结构化"之间切换
- 模式选择只在作者主动请求时出现，而非系统强制触发

### 两种写作状态

```
状态 A：自由书写（默认）
  作者打开编辑器，直接写。
  AI 在后台默默做：
    - 跟踪世界状态变化
    - 标记可能的 Step 边界（但不打断）
    - 更新角色位置和伏笔状态
  作者写完一段后按 Space 确认，AI 继续推进。
  没有"选模式"这一步。

状态 B：结构化写作（作者主动进入）
  作者按 [o] 打开大纲，看到 Step 列表。
  选择一个 Step，此时出现模式选择：
    [1] 我来写 → AI 扩写
    [2] AI 全写 → 我审阅
    [3] 我全写
    [4] 先整理卡片
  完成后自动跳到下一个 Step。
```

### 状态 A 的详细流程（心流模式）

```
作者开始写作
 │
 ├── 系统显示：上一段结尾 + 底部状态栏（当前章节、字数）
 │   （不展示 Step 信息、不展示推荐卡片——除非作者主动调出）
 │
 ├── 作者选择推进方式（通过快捷键，不需要进入菜单）：
 │    │
 │    ├─ 直接开始打字 → 进入人工写模式
 │    │    → 作者写完按 Enter 提交
 │    │    → AI 静默更新上下文，继续等待
 │    │
 │    ├─ 按 Space → AI 自动推进一段
 │    │    → AI 生成内容直接展示在正文区
 │    │    → 作者继续按 Space 让 AI 写下一段
 │    │    → 或按 e 进入编辑修改 AI 输出
 │    │    → 或直接开始打字覆盖
 │    │
 │    └─ 按 Tab → AI 基于作者刚写的内容扩写
 │         → 扩写结果追加到正文
 │         → 作者审阅：Space 接受 / e 编辑 / Backspace 撤销
 │
 └── 后台静默运行：
      - 世界状态更新
      - Step 边界自动标记（不暂停）
      - 一致性检查（不暂停，有问题记录到侧栏）
```

**关键区别**：没有"Step 开始 → 暂停 → 选模式 → 写 → Step 结束 → 暂停"的循环。作者感知到的是一个连续的写作流，系统在背后默默维护结构。

### 正文区的两种状态：浏览态与编辑态

自由书写模式下，正文区需要同时承担"展示已有文本"和"输入新文本"两个职责。为避免快捷键冲突（Space 是"AI 续写"还是"输入空格"？），正文区有两种明确状态：

```
浏览态（默认）：
  正文区只读，展示已有文本。
  Space / Tab / e / Backspace 等键是命令键。
  光标显示为块状 █，表示"命令等待"。

  按 Space → AI 续写
  按 Tab → AI 扩写（基于最后一段）
  按 e → 进入编辑态
  直接打字 → 自动进入编辑态，开始输入

编辑态（按 e 或直接打字进入）：
  正文区可输入，光标变为竖线 │。
  Space 就是空格，Tab 就是制表符——正常的文本输入行为。
  底部状态栏变为：[Enter]提交  [Esc]取消  [Ctrl+Tab]AI扩写

  按 Enter → 提交输入的文本，AI 静默更新上下文，回到浏览态
  按 Esc → 放弃输入，回到浏览态
  按 Ctrl+Tab → 在编辑态中请求 AI 扩写（不退出编辑态）
```

**状态转换图**：

```
          直接打字 / e
浏览态 ────────────────→ 编辑态
  ↑                         │
  └── Enter(提交) / Esc ────┘
```

这和 vim 的 normal/insert 模式类似——终端用户对此很熟悉。区别是进入编辑态不需要按 `i`，直接打字即可（更符合写作直觉）。

### 状态 B 的详细流程（结构化模式）

当作者需要全局规划时，按 `o` 进入大纲视图：

```
大纲视图
 │
 ├── 展示：Step 列表 + 完成状态 + 每个 Step 的简要描述
 │
 ├── 作者可以：
 │    ├─ 拆分/合并/插入/重排 Step
 │    ├─ 选择某个 Step 进入写作（此时展示模式选择）
 │    ├─ 给 Step 钉卡片
 │    └─ 返回自由书写
 │
 └── 离开大纲视图后，回到自由书写状态
```

### Step 边界的自动识别与手动调整

AI 在后台根据以下信号自动标记 Step 边界（不打断作者）：
- 场景切换（地点、时间变化）
- 视角切换（不同 POV 角色）
- 大段时间跳跃
- 作者插入的章节分隔符（如 `---` 或 `***`）

作者可以在大纲视图中看到自动标记的边界，手动确认、调整或忽略。

### 与现有机制的关系

- 自由书写模式中的 AI 推进复用 `checkpoint_on_subtask_complete` 机制，但**默认不暂停**（`auto_continue=True`）
- 结构化模式中的模式选择通过 AuthorGate 的新增 Intent 处理
- AI 扩写和审阅流程复用现有 HITL 的 APPROVE/REJECT/EDIT 流程
- 后台上下文更新复用 `ContextPlugin.merge_step_result()`

---

## TUI 改造

### 主界面（HomeScreen）— 心流优先布局

**核心改动**：正文区占据最大面积，结构信息默认隐藏，通过快捷键按需调出。

默认视图（自由书写状态）：

```
┌─────────────────────────────────────────────────┐
│  第三章 · 红岸基地                    8,432 字   │
│─────────────────────────────────────────────────│
│                                                 │
│  叶文洁站在发射塔前，寒风从大兴安岭的方向吹       │
│  来。她盯着眼前的面板，手指悬在发射按钮上方。      │
│                                                 │
│  二十年前的那个雪夜......                         │
│                                                 │
│  █                                              │
│                                                 │
│─────────────────────────────────────────────────│
│  Space:AI续写  Tab:扩写  e:编辑  ⚠ 1个一致性提示  │
└─────────────────────────────────────────────────┘
```

按 `\` 展开侧栏（按需调出，不常驻）：

```
┌─ 导航 ──────────┬─ 正文 ────────────────────────┐
│                  │                                │
│ ▼ 《三体》       │  第三章 · 红岸基地              │
│   ├─ 主线        │                                │
│   │  ├ Ch.1      │  叶文洁站在发射塔前，寒风       │
│   │  ├ Ch.2      │  从大兴安岭的方向吹来。她       │
│   │  └ Ch.3 ●    │  盯着眼前的面板，手指悬在       │
│   ├─ 支线A       │  发射按钮上方......             │
│   └─ 支线B       │                                │
│                  │  █                             │
│ ▼ 《新作》       │                                │
│   └─ 主线        │                                │
│                  ├────────────────────────────────┤
│──────────────────│ Space:AI续写 Tab:扩写 e:编辑   │
│ ⚠ 角色矛盾 Ch.3  │ o:大纲 m:卡片 \:收起侧栏      │
└──────────────────┴────────────────────────────────┘
```

**关键区别**：
- 默认视图没有侧栏，正文区全屏——像一个干净的编辑器
- 没有"Step 4/12"这样的进度提示——避免让作者觉得在"完成任务"而非"创作故事"
- 没有模式选择菜单——通过快捷键自然切换（直接打字=人写，Space=AI续写，Tab=扩写）
- 底部状态栏极简：只显示可用操作和需要注意的问题
- 一致性问题以 ⚠ 提示但不弹窗打断，作者选择何时处理

### 新增/改造 Screen

| Screen | 说明 |
|--------|------|
| `HomeScreen` | 改造：默认全屏正文区，侧栏按需展开 |
| `NovelCreateScreen` | 新增：极简创建（标题 + 开始写，世界观可稍后补充） |
| `OutlineEditScreen` | 新增：大纲编辑（拆分/合并/插入/重排 Step） |
| `ReaderScreen` | 新增：全屏阅读模式 |
| `CardPoolScreen` | 新增/改造自 MemoryScreen：卡片列表 + 关联展示 |
| `CardEditScreen` | 新增/改造自 MemoryEditScreen：卡片创建/编辑 + 关联管理 |
| `CharacterScreen` | 新增：角色档案管理 |
| `TimelineScreen` | 新增：时间线可视化 |
| `BranchCompareScreen` | 新增：支线对比 |
| `DashboardScreen` | 新增：作品全局概览 |
| `ExportScreen` | 新增/改造自 ArtifactListScreen：导出 |
| `SystemScreen` | 保留：内核状态展示 |

### 快捷键（心流优先设计）

写作状态下的快捷键极度精简——**常用操作不需要修饰键**：

**浏览态**（默认，正文只读，命令模式）：

| 按键 | 操作 |
|------|------|
| 直接打字 | 自动进入编辑态，开始输入 |
| `Space` | AI 续写一段 |
| `Tab` | AI 基于最后一段扩写 |
| `e` | 进入编辑态 |
| `Backspace` | 撤销上一段 AI 输出 |
| `Esc` | 退出当前操作 |

**编辑态**（按 e 或直接打字进入，正文可输入）：

| 按键 | 操作 |
|------|------|
| 正常打字 | 输入文本（Space=空格，Tab=制表符） |
| `Enter` | 提交输入，AI 静默更新上下文，回到浏览态 |
| `Esc` | 放弃输入，回到浏览态 |
| `Ctrl+Tab` | 在编辑态中请求 AI 扩写 |

导航和管理类快捷键（不常用，不影响心流）：

| 按键 | 操作 |
|------|------|
| `\` | 展开/收起侧栏 |
| `o` | 打开大纲编辑（进入结构化模式） |
| `r` | 全屏阅读模式 |
| `m` | 打开卡片池 |
| `c` | 打开角色档案 |
| `0` | 作品仪表盘 |
| `n` | 新建支线 |
| `N` | 新建作品 |
| `b` | 从当前节点分叉新支线 |
| `t` | 打开时间线 |
| `a` | 导出 |
| `i` | 系统状态 |
| `q` | 退出 |

---

## 实施阶段

### Phase 1a：能跑起来

> 目标：数据模型就位，最简交互能用。能创建作品，AI 能续写一段。

**1.1 新增 Novel 模型**
- `models/novel.py`：Novel ORM（title 必填，其余 nullable）
- `database.py`：migration，新增 `novels` 表
- `services/novel_service.py`：Novel CRUD

**1.2 扩展 CognitiveObject**
- 新增 `novel_id`, `parent_id`, `branch_point`, `pov`, `tone`, `word_count` 字段
- Migration 兼容：现有 CO 的 `novel_id` 默认为 NULL（向后兼容）

**1.3 扩展 Execution**
- 新增 `chapter`, `scene_title`, `content_text`, `author_draft`, `final_text`, `write_mode`, `accepted` 字段

**1.4 扩展 Memory**
- 新增 `novel_id` 字段
- 扩展 category 枚举

**1.5 新增 StepVersion 模型**
- 版本历史，每次生成/编辑自动 append
- 附带 `context_snapshot`

**1.6 修改 PromptPolicy**
- 替换系统提示词为写作导向
- 正文生成请求加入 `consistency_check` 输出要求
- 解析失败时静默通过 + 记录日志，不暂停写作

**1.7 极简 TUI + AI 续写**
- `NovelCreateScreen`：只需要标题
- `HomeScreen` 全屏正文区（浏览态），按 Space 让 AI 续写一段
- 底部极简状态栏
- 此阶段不实现编辑态、Tab 扩写、$EDITOR 集成——先保证"能写"

### Phase 1b：写作体验完整

> 目标：浏览态/编辑态切换流畅，三种输入方式（打字/Space/Tab）都可用。这是"30 秒内开始写作"的完整交付。

**1.8 浏览态/编辑态状态机（心流模式核心）**
- 浏览态：Space=AI续写，Tab=AI扩写，直接打字/e=进入编辑态
- 编辑态：正常文本输入，Enter=提交，Esc=取消，Ctrl+Tab=AI扩写
- 光标样式区分：浏览态 █ / 编辑态 │
- 底部状态栏随状态切换

**1.9 $EDITOR 外部编辑器集成**
- 实现 `$EDITOR` 调起逻辑（参考 `click.edit()`）
- Textual TextArea 作为 fallback
- config.yaml 新增 `editor` 配置节

**1.10 侧栏**
- 按 `\` 按需展开/收起
- 侧栏显示 ⚠ 一致性提示（此阶段只展示，不实现完整规则引擎）

### Phase 2：阅读 + 结构化写作 + 卡片入口

> 目标：作者写了几章后，自然需要回看、需要整理。在这个时刻提供工具。

**2.1 ReaderScreen（阅读模式）**
- 全屏阅读模式：将支线所有 final_text 按 Step 顺序拼接渲染
- j/k 滚动、翻页、文首/文末、从阅读位置进入编辑
- 按 `r` 进入

**2.2 OutlineEditScreen（大纲编辑 — 结构化模式入口）**
- 按 `o` 进入：展示 Step 列表 + 完成状态
- 支持拆分/合并/插入/重排 Step
- 在大纲视图中可选择某个 Step 进入结构化写作（此时出现模式选择）
- 离开后回到自由书写

**2.3 Step 边界自动识别**
- AI 在后台根据场景/视角/时间变化自动标记 Step 边界
- 不打断作者，在大纲视图中展示供确认

**2.4 世界观导入（可选，不强制）**
- Novel 编辑中可补充世界观描述
- 一次 LLM 调用拆解为结构化卡片（world_setting / character_profile / plot_rule）
- 作者审阅确认后批量写入 Memory
- 复用 MemoryExtractor 的 LLM judge 机制

**2.5 新增 MemoryLink 模型**
- 卡片间链接关系

**2.6 新增 StepCardRef 模型**
- Step 引用卡片

**2.7 CardPoolScreen**
- 列表视图（基于现有 MemoryScreen）
- 关联卡片展示
- 按 `m` 打开

**2.8 卡片钉入流程**
- 在大纲视图中给 Step 钉卡片（不在自由书写中主动弹出推荐）
- AI 生成时注入被钉卡片内容

**2.9 反向沉淀**
- 写作过程中一键提取文本为新卡片

**2.10 章节完成时生成 ChapterDigest**
- 一次 LLM 调用生成结构化摘要
- 存入 Storyline.context.chapter_digests[]

**2.11 版本列表 UI + 回退操作**
- Step 详情中展示 StepVersion 列表
- 回退时提供"仅回退文本" vs "回退文本 + 世界状态"两个选项

**2.12 DashboardScreen 基础版**
- 支线进展（Step 完成数）+ 字数统计
- 按 `0` 打开

### Phase 3：叙事引擎 + 长篇一致性

> 目标：后台守护叙事质量，不打断写作。所有检查异步进行，结果以 ⚠ 提示呈现。

**3.1 NarrativeEngine 规则层（后台静默执行）**
- L2：情节重复检测（文本相似度，重复度 > 70% 触发）
- L4：风格沙箱（POV 人称代词检查、句长统计）
- 纯 Python 实现，无 LLM 调用
- **检测到问题时：记录到侧栏 ⚠ 提示，不弹窗，不暂停**

**3.2 NarrativeEngine 批量审查（章节间隙执行）**
- 章节完成后用经济模型做全面审查
- 结果存入 NarrativePerception，下一章写作时作为约束注入
- **不阻塞写作过程**

**3.3 创作决策分级 — 极度偏向 AUTO**
- L3：AUTO/NOTIFY/CONFIRM/APPROVE 边界定义
- 初始策略：**几乎所有内容 AUTO**，只有以下情况 CONFIRM：
  - AI confidence < 0.3（不是 0.5，提高 AUTO 覆盖）
  - 涉及角色死亡（通过关键词检测）
  - 显式违反已有 plot_rule
- NOTIFY 替代大部分原 CONFIRM 场景：生成后在侧栏通知，不暂停
- 根据作者审阅行为自适应调整边界

**3.4 NarrativePerception（后台感知，不打断）**
- 内容类型接受率统计
- 叙事节奏检测
- 伏笔到期提醒（注入到 prompt，不弹窗提醒作者）
- 角色出场平衡

**3.5 ContextPlugin 改造**
- 近章全文 + 远章摘要链
- Token 预算分配机制（固定优先级裁决，超出预算从底部裁剪）
- 按需原文回溯（needs_recall）

**3.6 后台 LLM 卡片关联发现**
- 触发：新卡片创建时 / 作者暂停写作超 30 秒时
- 结果存入 MemoryLink（source=ai_suggested）

**3.7 偏好持久化**
- 稳定的审阅模式写入 Memory

### Phase 4：TUI 深度打磨

> 目标：打磨体验细节。在此之前系统已经"能用"，这一阶段让它"好用"。

**4.1 正文展示区增强**
- 富文本渲染（Markdown）
- 支持滚动回看
- 当前写作位置高亮

**4.2 ReaderScreen 增强**
- 段落缩进、章节跳转、字数统计

**4.3 Novel/Storyline 树形视图**
- 替换 COList 为 Tree widget（在侧栏中）
- 支持 Novel → Storyline → Chapter 三级展开

**4.4 支线分叉与对比**
- 一键从当前 Step 分叉新支线
- BranchCompareScreen 对比两条支线

**4.5 DashboardScreen 完整版**
- 伏笔追踪、角色出场统计、写作活动统计

**4.6 CardPoolScreen 增强**
- 区分手动关联（实线） vs AI 建议（虚线）

**4.7 导出**
- 按章节导出 Markdown
- 按支线导出完整文本
- 全书导出

### Phase 5：高级特性

**5.1 卡片图谱视图**
- 可视化卡片链接关系网络

**5.2 时间线视图**
- 按 plot_beats 可视化叙事时间线
- 跨支线时间线对比

**5.3 角色关系图**
- 基于 character_profile 和剧情自动生成角色关系

**5.4 多模型路由**
- 主推理用高能力模型（写正文）
- 经济模型用于一致性检查、上下文压缩、大纲建议

---

## 补充设计：七个关键问题的解决方案

以下是对上述架构设计的深化——每个问题对应一个具体的实现方案。其落地步骤已合并进"实施阶段"的各 Phase 中。

### 问题 1：叙事引擎的检查靠什么——规则还是 LLM？

防火墙的五层检查有明确依据（Schema 校验、路径白名单），是确定性判断。但"这段叙事是否违反世界观"是模糊判断。如果五层全调 LLM，每个 Step 的延迟和成本翻五倍，写作心流归零。

**方案：三级判定策略——规则优先，LLM 内嵌，批量后置**

```
即时规则检查（零成本，不阻塞）:
  L2 情节重复  → 文本相似度（difflib/编辑距离），重复度 > 70% 触发
  L3 决策分级  → 关键词规则表（"死""杀""永别" → APPROVE，"走到""说道" → AUTO）
  L4 风格沙箱  → POV 人称代词检查（第一人称支线不应出现"他想"）
                 句长统计（设定"短句风格"时平均句长超阈值则警告）

即时 LLM 检查（零增量成本，内嵌在生成请求中）:
  L1 设定一致性 + L5 叙事质量 → 合并到正文生成的同一次 LLM 调用
  要求输出包含 consistency_check 字段：
  {
    "content": "正文...",
    "consistency_check": {
      "violations": [],
      "confidence": 0.85,
      "quality_concern": null,
      "character_changes": {},
      "new_foreshadowing": []
    },
    "needs_recall": null
      // 非空时触发按需原文回溯，见问题 6 补充方案
      // 示例：{"query": "第5章中铁门上的符号描述", "reason": "当前段落涉及角色再次面对这扇门"}
  }
  不额外调用，零增量成本。

  **容错策略**：`consistency_check` 解析失败时**静默通过 + 记录日志**，不暂停写作。
  理由：防火墙场景下解析失败必须保守（`human_required=True`），因为后果是执行危险操作。
  写作场景下最坏后果是漏掉一个设定矛盾，延迟批量检查可以兜底。
  频繁因 JSON 格式错误打断作者会摧毁心流。

延迟批量检查（章节完成后，后台执行）:
  每完成一个章节，用经济模型做一次全面审查：
  - 完整章节 vs 世界观设定的矛盾扫描
  - 角色行为一致性（性格是否突变）
  - 伏笔状态更新
  结果存入 NarrativePerception，下一章写作时作为约束注入。
  不阻塞写作过程。
```

**落地步骤**：
1. Phase 1a：正文生成请求里加 `consistency_check` 输出要求，解析后记录
2. Phase 3：实现 L2/L4 的规则引擎（纯 Python，无 LLM 调用）
3. Phase 3：实现章节完成后的批量审查任务

### 问题 2：正文在哪里编辑

三种写作模式都涉及作者输入长文本。终端里的 TextArea 不是合格的文本编辑器——没有撤销历史、没有选词替换。这会直接劝退写作者。

**方案：$EDITOR 外部编辑器为主，内置 TextArea 为辅**

```
作者在编辑态下按 Esc 后选择调用外部编辑器，
或在结构化模式中选择 [1]我来写 时：

默认行为（$EDITOR 模式）：
  1. 生成临时文件 /tmp/overseer_step_{id}.md
  2. 文件头部写入只读的上下文提示（注释包裹）：
     <!-- 场景：宝黛初会 | 上一段结尾：...贾母笑道... -->
     <!-- 钉入卡片：[角色]林黛玉 [角色]贾宝玉 -->
     （以下开始写作）
  3. 调起 $EDITOR（vim/nano/vscode）
  4. 用户保存退出后，系统读取内容（去掉注释头）
  5. 模式 1 则将内容作为 author_draft 提交给 AI 扩写

回退行为（无 $EDITOR 或配置 editor: builtin）：
  使用 Textual TextArea，适合短文本输入

配置项：
  editor:
    mode: external          # external | builtin
    command: "vim"           # 覆盖 $EDITOR
    template: true           # 是否写入上下文提示头
```

类似 git commit 的编辑体验——终端用户对这种模式很熟悉。

**落地步骤**：
1. Phase 1b：实现 `$EDITOR` 调起逻辑（参考 `click.edit()`）
2. Phase 1b：Textual TextArea 作为 fallback
3. config.yaml 新增 `editor` 配置节

### 问题 3：Step 缺少版本历史

Step 只有三个文本字段，重写后旧版本丢失。"之前那个版本写得更好"是写作中的高频需求。

**方案：StepVersion 追加表**

```
StepVersion（新增模型）
  id: UUID
  step_id: FK → Execution
  version_number: int          # 自增
  source: str                  # ai_generated / author_draft / author_edit / ai_expand
  content: Text                # 该版本的完整文本
  created_at: datetime

操作对应：
  AI 生成   → append version(source=ai_generated)
  作者草稿  → append version(source=author_draft)
  AI 扩写   → append version(source=ai_expand)
  作者编辑  → append version(source=author_edit)
  作者接受  → Step.final_text = 该版本的 content

回退：
  作者在 Step 详情中看到版本列表
  选择任意版本 → 设为 final_text
  不删除其他版本
```

直接存全量文本。单个 Step 几千字，版本数通常 < 10，存储可忽略。

**版本回退与上下文一致性**

回退单个 Step 的文本后，后续 Step 的上下文（世界状态、角色位置、伏笔状态）仍基于旧版本生成，产生不一致。解决方案：每个 StepVersion 附带上下文快照。

```
StepVersion 扩展：
  context_snapshot: JSON (nullable)
    - Step 完成时，将当时的 Storyline.context 快照存入
    - 回退时，作者可选择是否同时回退世界状态到该快照
    - 不自动回退（避免破坏后续已确认的内容）
    - 提供选项："仅回退文本" vs "回退文本 + 世界状态"
```

这样回退操作是可控的：作者知道自己在做什么，系统不做隐式的连锁修改。

**落地步骤**：
1. Phase 1a：新增 `StepVersion` 模型
2. Phase 1a：每次生成/编辑时自动 append
3. Phase 2：TUI 里加版本列表和回退操作

### 问题 4：没有阅读模式

所有流程围绕"写"，但写作中大量时间在"读"——读前文找感觉，读整条支线判断节奏。

**方案：ReaderScreen——全屏阅读模式**

```
按 [r] 进入：

┌─────────────────────────────────────────────┐
│  《三体》 · 主线 · 第三章                     │
│─────────────────────────────────────────────│
│                                             │
│  叶文洁站在发射塔前，寒风从大兴安岭的方       │
│  向吹来。她盯着眼前的面板，手指悬在发射       │
│  按钮上方。                                  │
│                                             │
│  二十年前的那个雪夜......                     │
│                                             │
│─────────────────────────────────────────────│
│  Ch.3 Step 4/12 │ 8,432 字 │ [e]编辑 [q]返回 │
└─────────────────────────────────────────────┘

操作：
  j/k ↑↓      逐行滚动
  PgUp/PgDn   翻页
  g/G          文首/文末
  数字+Enter   跳到指定章节
  e            从阅读位置进入编辑
  q/Esc        返回
```

将支线所有 `final_text` 按 Step 顺序拼接渲染。

**落地步骤**：
1. Phase 2：实现 ReaderScreen 基础版
2. Phase 4：增强排版（段落缩进、章节跳转）
3. 快捷键新增 `r`

### 问题 5：卡片关联发现能力弱

卡片有链接机制，但依赖作者手动关联。卡片的核心价值是发现意外联系。

**方案：后台关联发现 + 写作时上下文推荐**

```
被动发现（后台，不阻塞）：
  触发：新卡片创建时 / 作者暂停写作超 30 秒时
  流程：
    1. 取最近的卡片 A
    2. 从 CardPool 同作品中取候选（tag 粗筛，限 20 张）
    3. 一次经济模型调用：判断 A 与候选之间的潜在叙事联系
    4. 结果存入 MemoryLink，标记 source=ai_suggested
    5. CardPoolScreen 中 AI 建议显示为虚线（vs 手动关联的实线）
    6. 作者确认（变实线）或删除

主动推荐（在结构化模式的大纲视图中选择 Step 时）：
  1. 从 Step 大纲提取关键词（角色名、地点、事件）
  2. tag 匹配 + MemoryLink 链接扩展 → 候选集
  3. 候选 > 5 张时用 LLM 排序
  4. 展示 top 3-5 张，一键钉入
  注意：不在自由书写模式中主动弹出推荐——那会打断心流。
```

**落地步骤**：
1. Phase 2：结构化模式中选择 Step 时的关键词匹配推荐（纯规则）
2. Phase 3：后台 LLM 关联发现
3. Phase 4：CardPoolScreen 区分手动关联和 AI 建议

### 问题 6：长篇小说的上下文一致性

10 万字装不进 context window。写到第 30 章时，AI 不记得第 2 章的细节。关键词检索也不够——第 2 章的伏笔关键词不会出现在第 30 章的 query 里。

**方案：章节摘要链 + 显式伏笔追踪 + 角色状态快照**

```
章节摘要链（ChapterDigest）：
  每完成一个章节，一次 LLM 调用生成结构化摘要：
  {
    "chapter": 3,
    "summary": "叶文洁在红岸基地做出了向宇宙发射信号的决定...",
    "key_events": ["叶文洁按下发射按钮", "雷志成发现异常信号记录"],
    "character_changes": {"叶文洁": "从犹豫转为决绝"},
    "new_foreshadowing": ["异常信号记录未被销毁"],
    "resolved_foreshadowing": [],
    "world_state_delta": {"红岸基地": "信号已发射"}
  }

  存储：Storyline.context.chapter_digests[]
  后续章节 prompt 加载：最近 2 章全文 + 更早章节的摘要链

显式伏笔追踪：
  埋设：
    - consistency_check 输出中检测到 → 自动记录
    - 或作者手动标记
    - 写入 context.foreshadowing[]

  提醒：
    - 每个 Step 开始时检查未解决伏笔
    - 超过阈值（同章 5 Step / 跨章 2 章）→ 注入提醒到 prompt
    - 阈值可配置

  回收：
    - consistency_check 中声明 resolved
    - 或作者手动标记
    - foreshadowing[].resolved = true

角色状态快照：
  每个 Step 完成后，从 consistency_check 提取 character_changes
  更新 context.character_arcs
  后续 prompt 注入角色当前状态，而非让 AI 从全文推断
```

**补充方案：按需原文回溯**

摘要链是有损压缩。第 2 章的摘要不可能保留所有细节——一个角色的口头禅、一段对话的具体措辞、一个场景里的天气，这些信息在摘要中被丢弃。当第 30 章需要引用这些细节时，摘要链无法提供。

不引入向量数据库。原因：写作中需要的跨章回溯是**叙事关联**（"他打开那扇门"→ 需要第 5 章对这扇门的描写），不是**语义相近**（向量搜索擅长的）。向量搜索对叙事关联的召回率不可靠，且引入额外依赖（embedding 调用、向量存储、数据同步）的成本不值得。

方案是利用已有的结构化索引做按需精确回溯：

```
按需原文回溯（needs_recall）：
  触发：AI 生成正文时输出 needs_recall 字段（非空）

  输出格式（内嵌在生成请求的输出要求中，零增量成本）：
  {
    "content": "正文...",
    "consistency_check": {...},
    "needs_recall": {
      "query": "第5章中铁门上的符号描述",
      "reason": "当前段落涉及角色再次面对这扇门"
    }
  }

  处理流程（ContextPlugin 负责）：
    1. 解析 needs_recall.query
    2. 从结构化索引定位相关 Step：
       - plot_beats 中匹配关键词（铁门、符号）
       - foreshadowing 中匹配相关伏笔
       - character_arcs 中匹配相关角色
       - chapter_digests 中匹配章节号
    3. 加载定位到的 Step 的 final_text 原文
    4. 将原文片段注入 context，重新生成当前 Step
       （或作为补充信息追加到下一轮生成中）

  容错：
    - needs_recall 解析失败 → 静默忽略，同 consistency_check 的容错策略
    - 未定位到相关 Step → 记录日志，不阻塞写作
    - 大部分 Step 不会触发回溯，摘要链已足够
```

这个机制的关键：**不做全量索引，不预计算 embedding，只在 AI 主动请求时按需查询 SQLite**。成本完全可控——偶尔多一次数据库查询和一次重新生成，远低于每个 Step 都做 embedding 的固定开销。

**落地步骤**：
1. Phase 1a：consistency_check 中包含 character_changes 和 foreshadowing
2. Phase 2：章节完成时生成 ChapterDigest
3. Phase 3：ContextPlugin 改为"近章全文 + 远章摘要链"
4. Phase 3：伏笔到期提醒
5. Phase 3：按需原文回溯（needs_recall 解析 + Step 定位 + 原文加载 + 重新生成）

### 问题 7：缺少全局视图

作者始终处于局部视角。需要一个纵观全局的地方。

**方案：DashboardScreen——作品级全局概览**

```
按 [0] 进入：

┌─────────────────────────────────────────────┐
│  《三体》 · 仪表盘                            │
│─────────────────────────────────────────────│
│                                             │
│  支线进展                    字数统计         │
│  ─────────                  ─────────       │
│  主线   ████████░░ Ch.8/12   52,340 字      │
│  支线A  ████░░░░░░ Ch.3/8    18,200 字      │
│  支线B  ██░░░░░░░░ Ch.1/5     6,100 字      │
│                              总计 76,640 字   │
│                                             │
│  未解决伏笔 (4)              角色最后出场     │
│  ─────────────              ──────────────   │
│  · 异常信号记录  Ch.3 已过7章  叶文洁  Ch.8   │
│  · 申玉菲的日记  Ch.5 已过3章  大史    Ch.7   │
│  · 三体游戏邀请  Ch.6 已过2章  汪淼    Ch.8   │
│  · 倒计时       Ch.7 已过1章  申玉菲  Ch.5 ⚠ │
│                                             │
│  近期活动                                    │
│  ────────                                   │
│  今日 3 Step · 2,100 字 · AI写/人写 = 2/1   │
│  本周 12 Step · 8,400 字                     │
│─────────────────────────────────────────────│
│  [s]写作  [m]卡片池  [r]阅读  [q]返回        │
└─────────────────────────────────────────────┘
```

数据全部来自已有数据的聚合查询，无需新模型：
- 支线进展：Plan 的 Step 完成数
- 字数：sum(final_text 字数) per Storyline
- 伏笔：context.foreshadowing where resolved=false
- 角色出场：遍历 plot_beats 取 characters 最大 chapter
- 角色超过 N 章未出现标注 ⚠

**落地步骤**：
1. Phase 2：基础版（支线进展 + 字数）
2. Phase 3：加入伏笔追踪和角色出场
3. Phase 4：写作活动统计

---

## 风险与约束

### 风险 1：系统太"重"导致无人使用

这是最大的风险。Novel → Storyline → Plan → Step → StepVersion，加上 Memory / MemoryLink / StepCardRef，加上五层 NarrativeEngine，加上 NarrativePerception……

**一个作者坐下来想写小说，如果面对的是一个需要先创建作品、导入世界观、审阅 AI 拆出的卡片、确认大纲、选择写作模式、然后才能写第一段话的系统，他会关掉它打开一个空白文本文件。**

应对策略：
- Phase 1b 的验收标准是"30 秒内写出第一段话"
- 所有结构（大纲、卡片、世界观）都是可选的，不是前置的
- 系统在后台默默建立结构，在作者需要时呈现

### 风险 2：打断太多摧毁心流

初始策略：**极度偏向 AUTO**（confidence 阈值 0.3 而非 0.5），只在角色死亡、显式违反 plot_rule 时 CONFIRM。宁可漏掉一个设定矛盾（延迟批量检查兜底），也不要在作者写到精彩处时弹出确认框。

用 NOTIFY（侧栏 ⚠ 提示）替代大部分原 CONFIRM 场景。作者选择何时处理。

### 风险 3：Step 粒度与作者节奏不匹配

AI 切分出来的 Step 几乎一定和作者脑中的节奏不一致。应对：
- Phase 1a/1b 不强制 Step 结构，默认自由书写
- Step 边界由 AI 在后台自动标记，作者在大纲视图中按需确认/调整
- Phase 2 提供完整的拆分/合并/重排能力

### 风险 4：长文本展示是 TUI 的短板

Textual 的 RichLog/TextArea 可以展示长文本，但终端宽度有限。需要在 Phase 4 仔细调试排版。如果终端体验不够好，可能需要考虑可选的 Web 预览。

### 风险 5：consistency_check 实际可靠性

LLM 在写长文本时的注意力主要在叙事上，附带的结构化元数据（consistency_check）容易出错或遗漏。文档的容错策略是"解析失败时静默通过"——这意味着实际运行中一致性检查可能经常形同虚设。

应对：
- 不依赖即时 consistency_check 作为唯一防线
- 延迟批量检查（章节完成后）作为真正的兜底
- 规则引擎（L2/L4）做确定性检查，不依赖 LLM

### 风险 6：长篇上下文的"摘要链"天花板

摘要是有损压缩——角色的口头禅、某段对话的具体措辞、一个不经意的细节，在摘要中被丢弃。needs_recall 按需回溯机制依赖 AI 自己意识到需要回溯，而 AI 不知道自己不知道什么。

这是已知局限，不引入向量数据库（叙事关联不是语义相似）。摘要链 + 显式伏笔追踪 + needs_recall 是当前最务实的方案。

### 小说写作是完全替换，不是兼容模式

本次改造是对现有防火墙语义的完全替换。不保留"AI 任务执行"模式，不做双模式兼容。CO 就是 Storyline，Execution 就是 Step，Memory 就是素材卡片。所有命名、提示词、TUI 文案、交互流程统一切换为写作语义。旧数据（如果有）通过 migration 清理或归档，不做向后兼容。
